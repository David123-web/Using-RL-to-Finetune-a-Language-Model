reward_model:
  type: "sentiment"
  name: "distilbert-base-uncased-finetuned-sst-2-english"

weights:
  sentiment: 1.0
  repetition: -0.2           # Reduced penalty from -0.5
  length: 0.2                # Increased bonus from 0.1

length_target:
  min_tokens: 10
  max_tokens: 40